import 'package:flutter/widgets.dart';

class {{name.pascalCase()}}LoadingWidget extends StatelessWidget {
  const {{name.pascalCase()}}LoadingWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}